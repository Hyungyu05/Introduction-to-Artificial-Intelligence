# 보안 파일 (절대 올리면 안 됨)
.env
.env.*

# 파이썬 캐시 및 가상환경
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.vscode/

# 데이터베이스 및 로그 (로컬에서만 쓰는 것들)
*.duckdb
*.wal
*.tmp
logs/
data/
!data/.gitkeep

# (선택) data 폴더 구조는 유지하되 내용물만 빼고 싶다면 위 data/ 대신 아래처럼 쓰세요:
# data/*.duckdb
# data/backups/
# data/logs/
# data/exports/